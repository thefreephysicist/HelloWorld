pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Get some code from a GitHub repository
                echo "Hello world!"
            }

            post {
            }
        }
        stage('Test') {
            steps {
                echo "Test echos"
            }
        }
    }
}
